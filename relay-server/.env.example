# Copy this file to .env and fill in the two required values (see README.md
# for how to find them). .env is gitignored - never commit your cookie.

CLAUDE_ORG_ID=
CLAUDE_SESSION_COOKIE=

# Optional overrides
RELAY_PORT=8787
RELAY_BIND_HOST=0.0.0.0
RELAY_POLL_INTERVAL_SECONDS=300
